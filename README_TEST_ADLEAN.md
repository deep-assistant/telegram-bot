# Тестовые скрипты для проверки Adlean API

## 📝 Описание

Два автономных скрипта для проверки работы рекламного API Adlean без запуска бота.

## 📦 Файлы

### 1. `test_adlean_api.py`
**Простой тест** - отправляет один запрос и выводит детальную информацию.

**Запуск:**
```bash
python3 test_adlean_api.py
```

**Что показывает:**
- Отправленный payload (JSON)
- Полный ответ от API
- Статус, время ответа
- Рекламный контент (если есть)

---

### 2. `test_adlean_api_multiple.py`
**Множественные запросы** - отправляет несколько запросов подряд от одного пользователя.

**Запуск:**
```bash
python3 test_adlean_api_multiple.py
```

**Что делает:**
- Отправляет 4 разных запроса
- Имитирует поведение реального пользователя
- Проверяет показ рекламы (согласно конфигу, показывается после 2-го запроса)

---

## 🔧 Настройки

Скрипты используют настройки из `config.py`:
- `ADLEAN_API_KEY` - API ключ для аутентификации
- `ADLEAN_API_URL` - эндпоинт API (`https://api.adlean.pro/engine/send_message`)

Для изменения тестовых запросов отредактируйте переменные:
- `TEST_MESSAGE` в `test_adlean_api.py`
- `TEST_MESSAGES` в `test_adlean_api_multiple.py`

---

## 📊 Формат ответа API

```json
{
  "have_ads": false,          // Есть ли реклама для показа
  "content": null,            // Текст рекламы (если have_ads = true)
  "show_price": 0.0,          // Стоимость показа
  "click_price": 0.0          // Стоимость клика
}
```

---

## ℹ️ Важные заметки

1. **Реклама не всегда показывается** - это нормально! API может вернуть `have_ads: false` по разным причинам:
   - Нет активных рекламных кампаний
   - Не подходит таргетинг
   - Превышен дневной бюджет
   - Реклама показывается только после N-го запроса

2. **Скрипты полностью автономны** - не требуют запуска бота, используют только стандартные библиотеки Python

3. **Для реальной интеграции** смотрите `services/adlean_service.py` - там полная реализация с обработкой ошибок

---

## 🔍 Проверка API в браузере

Документация: https://api.adlean.pro/docs

---

## 🐛 Устранение проблем

**Ошибка "ModuleNotFoundError":**
- Скрипты используют только стандартные библиотеки Python (urllib, json, time)
- Должны работать на любой системе с Python 3.6+

**API не отвечает:**
- Проверьте интернет-соединение
- Убедитесь, что API ключ актуален
- Проверьте доступность https://api.adlean.pro/

**Реклама не показывается:**
- Это нормально! См. раздел "Важные заметки"
- Попробуйте разные тексты запросов
- Проверьте настройки в конфиге (`ADLEAN_SHOW_AFTER_N_REQUESTS`)

