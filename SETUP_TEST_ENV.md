# 🧪 Настройка тестовой версии бота

Это руководство для развертывания тестовой версии бота на отдельном сервере с локальным GPT API.

---

## 📋 Архитектура

### **Production (текущий сервер)**
```
Telegram Bot ──► https://api.deep.assistant.run.place (GPT API)
             └─► https://api.adlean.pro (AdLean API)
```

### **Test (новый сервер)**
```
Test Bot ──► http://localhost:8000 (Локальный GPT API)
         └─► https://api.adlean.pro (AdLean TEST API)
```

---

## 🔧 ШАГ 1: Создание тестового бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (например, имя: `MyBot Test`)
4. Сохраните полученный **TOKEN**

**Пример:**
```
6497291416:AAGbkPn3AqEZkX8yp2gvCS-HKKlHIBlFU-E  ← Production
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789  ← Test (новый)
```

---

## 🗂️ ШАГ 2: Настройка базы данных

**ВАЖНО:** Тестовый и продакшн боты должны использовать **разные файлы БД**!

### Вариант А: Изменить `db/init_db.py`

```python
import os
import vedis

# Используем разные файлы БД для prod/test
db_name = os.getenv('DB_NAME', 'data_base.db')
data_base = vedis.Vedis(os.path.join(os.getcwd(), db_name))

def db_key(user_id, key):
    return f"{user_id}_{key}"
```

### Вариант Б: Hardcode для теста

```python
# Для тестового сервера
data_base = vedis.Vedis(os.path.join(os.getcwd(), 'data_base_test.db'))
```

---

## ⚙️ ШАГ 3: Конфигурация для тестовой версии

### Создайте `config.py` на тестовом сервере:

```python
# ========== ТЕСТОВАЯ ВЕРСИЯ ==========

# Telegram Bot
TOKEN = "ТЕСТОВЫЙ_BOT_TOKEN"  # От @BotFather

# Локальный GPT API (на том же сервере)
PROXY_URL = "http://localhost:8000"  # ← Локальный адрес!
ADMIN_TOKEN = "test_admin_token_12345"  # Отдельный для теста

# Analytics
ANALYTICS_URL = "https://6651b4300001d.tgrasp.co"

# API Keys
KEY_DEEPINFRA = "dChpwTq4VSmDBI8yxa3MVZzapqQNapNx"
GO_API_KEY = "46ce8fc6e406b777b232ca2506cd3f0d7953d0d80a835f42b3b404c6793cbef9"
GUO_GUO_KEY = "sk-shyP9qKCL36w7Yip87C1E419551f48498156Dc5998316cBe"
OPENROUTER_API_KEY = "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"

# Payments - ОБЯЗАТЕЛЬНО TEST токен!
PAYMENTS_TOKEN = "390540012:TEST:xxxxx"  # ← TEST, не LIVE!

# Development
IS_DEV = True

# Webhook (для теста обычно False)
WEBHOOK_ENABLED = False
WEBHOOK_URL = "https://your.domain.com/webhook"
WEBHOOK_PATH = "/webhook"
WEBHOOK_HOST = "0.0.0.0"
WEBHOOK_PORT = 3000

# AdLean Integration - Тестовая конфигурация
ADLEAN_API_KEY = "ad-TEST_KEY_если_есть"  # Попросите тестовый у NextUP
ADLEAN_API_URL = "https://api.adlean.pro/engine/send_message"
ADLEAN_ENABLED = True  # или False для отключения рекламы в тесте
ADLEAN_SHOW_AFTER_N_REQUESTS = 1  # Для быстрого тестирования (вместо 2)
```

---

## 🚀 ШАГ 4: Запуск локального GPT API

### Проверка что API запущен:

```bash
# Проверка доступности
curl http://localhost:8000/health

# Или
curl http://localhost:8000/
```

**Должен вернуть:** Status 200 OK

### Если API еще не запущен:

```bash
# Перейдите в директорию с GPT API
cd /path/to/gpt-api

# Запустите API (пример)
python main.py  # или
uvicorn main:app --host 0.0.0.0 --port 8000
```

---

## 📦 ШАГ 5: Развертывание тестового бота

### 1. Клонируйте репозиторий на тестовый сервер:

```bash
git clone https://github.com/deep-assistant/telegram-bot.git test-bot
cd test-bot
```

### 2. Создайте `config.py` (см. ШАГ 3)

```bash
nano config.py
# Вставьте конфигурацию из ШАГ 3
```

### 3. Установите зависимости:

```bash
pip install -r requirements.txt
```

### 4. Проверьте что БД файл отличается:

```bash
# В db/init_db.py должно быть:
# data_base = vedis.Vedis('data_base_test.db')
# или используйте переменную окружения
export DB_NAME=data_base_test.db
```

### 5. Запустите бота:

```bash
python __main__.py
```

---

## ✅ ШАГ 6: Проверка работоспособности

### Проверьте Health Check локального API:

Используйте функции из `services/utils.py`:

```python
from services.utils import check_api_health, wait_for_api

# В bot_run.py перед запуском бота:
async def bot_run():
    # Проверяем доступность локального API
    if not await check_api_health("http://localhost:8000"):
        print("❌ ОШИБКА: Локальный GPT API не доступен!")
        print("Запустите GPT API перед запуском бота")
        return
    
    # Остальной код...
```

### Тестирование бота:

1. **Отправьте `/start` в тестового бота**
2. **Отправьте текстовый запрос** (например: "Привет!")
3. **Проверьте логи бота:**
   - Должно быть: `Sending POST request to http://localhost:8000/completions`
   - НЕ должно быть: `https://api.deep.assistant.run.place`

4. **Проверьте рекламу:**
   - Отправьте 2 запроса подряд
   - После 2-го должна показаться реклама (если `ADLEAN_ENABLED = True`)

---

## 🔍 Отладка проблем

### Проблема 1: "Не могу подключиться к API"

**Ошибка:**
```
[Health Check] ❌ Не удалось подключиться к http://localhost:8000
```

**Решение:**
```bash
# Проверьте что GPT API запущен
ps aux | grep python
netstat -tulpn | grep 8000

# Проверьте доступность
curl http://localhost:8000/health
```

### Проблема 2: "База данных перемешалась с production"

**Ошибка:**
```
Пользователи из prod появляются в тесте
```

**Решение:**
```bash
# Убедитесь что используется другой файл БД
ls -la data_base*.db

# Должно быть:
# data_base.db       ← Production
# data_base_test.db  ← Test
```

### Проблема 3: "Платежи идут на реальный счет"

**КРИТИЧНО!**

**Проверьте:**
```python
PAYMENTS_TOKEN = "390540012:TEST:xxxxx"  # Должен быть :TEST: !
```

**Если стоит `:LIVE:` - НЕМЕДЛЕННО ОСТАНОВИТЕ БОТА!**

---

## 📊 Мониторинг

### Логи запросов к локальному API:

```bash
# В логах бота должно быть:
grep "PROXY_URL" bot.log
# Результат: http://localhost:8000

# НЕ должно быть:
# https://api.deep.assistant.run.place
```

### Проверка латентности:

```bash
# Локальный API должен быть БЫСТРЕЕ
time curl -X POST http://localhost:8000/completions \
  -H "Content-Type: application/json" \
  -d '{"content": "test"}'

# Должно быть: <1s (vs 2-5s для внешнего API)
```

---

## 🔐 Безопасность

### ⚠️ ВАЖНО для тестового сервера:

1. **НЕ используйте production токены** (TOKEN, PAYMENTS_TOKEN)
2. **НЕ используйте production базу данных** (data_base.db)
3. **НЕ используйте LIVE платежный токен**
4. **Используйте отдельный ADMIN_TOKEN** для разделения аналитики

### Рекомендуется:

```bash
# Добавьте в .gitignore
echo "config.py" >> .gitignore
echo "data_base*.db" >> .gitignore

# Никогда не коммитьте секреты!
```

---

## 🔄 Синхронизация с production

### Обновление кода из GitHub:

```bash
cd /path/to/test-bot
git pull origin main

# ВАЖНО: НЕ перезаписывайте config.py!
# Он должен оставаться с тестовыми настройками
```

### Перед деплоем в production:

1. ✅ Протестируйте в тестовой версии
2. ✅ Проверьте логи на ошибки
3. ✅ Убедитесь что реклама работает
4. ✅ Сделайте `git commit` и `git push`
5. ✅ На production сервере: `git pull`

---

## 📝 Чеклист перед запуском

- [ ] Создан тестовый бот через @BotFather
- [ ] `TOKEN` - новый тестовый (не production)
- [ ] `PROXY_URL` - `http://localhost:8000`
- [ ] `PAYMENTS_TOKEN` - с `:TEST:` (НЕ `:LIVE:`)
- [ ] База данных - `data_base_test.db` (не `data_base.db`)
- [ ] `ADMIN_TOKEN` - отдельный для теста
- [ ] `ADLEAN_API_KEY` - тестовый (или отключен)
- [ ] GPT API запущен и доступен на `localhost:8000`
- [ ] `config.py` в `.gitignore`
- [ ] Health check локального API прошел успешно

---

## 🛠️ Полезные команды

```bash
# Проверка статуса бота
ps aux | grep python

# Просмотр логов в реальном времени
tail -f bot.log

# Проверка локального API
curl http://localhost:8000/health

# Проверка портов
netstat -tulpn | grep 8000

# Остановка бота
pkill -f __main__.py

# Перезапуск с логированием
python __main__.py 2>&1 | tee bot.log
```

---

## 📧 Контакты для помощи

**Проблемы с локальным API?**
- Проверьте что API запущен: `ps aux | grep api`
- Логи API: `tail -f api.log`

**Проблемы с рекламой?**
- Свяжитесь с NextUP Media для тестового API ключа

**Общие вопросы?**
- GitHub Issues: https://github.com/deep-assistant/telegram-bot/issues

---

**Последнее обновление:** 2025-11-17

